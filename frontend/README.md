# tunes frontend

Vite + React + TypeScript SPA. See the [root README](../README.md) for setup
and run instructions.

Talks to the backend at `/api/*`, proxied to `http://localhost:8000` in dev
(see `vite.config.ts`).
